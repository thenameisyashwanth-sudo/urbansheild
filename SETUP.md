# UrbanShield 2.0 — Setup

```bash
npm create vite@latest urbanshield -- --template react
cd urbanshield
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
npm install firebase react-router-dom lucide-react
```

Then replace the generated files with the ones provided.
